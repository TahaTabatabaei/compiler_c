#include "lib.h"

const double pointer = 10.11;
void myFunction(int myNumbers[5], char isOdd) {
    for (int i = 0; i < 5; i++) {
    if(isOdd == 'y'){
    printf("%d\n", myNumbers[i]);
    while(false){
    int nested = 2;
    }
    }
    }
}
char c = 'p' , a ='a';
int main() {
    double point = 1.9;
    int myNumbers[5] = {10, 20, 30, 40, 50};
    double point = 2.3;
    point = 2;
    myFunction(myNumbers);

    double data = myFunction(myNum);
    return 0;
    }
